🔹 Self-Sovereign Identity (SSI)

✔ Data hanya disimpan oleh pengguna, bukan oleh server.

🔹 Zero-Knowledge Proof Authentication

✔ Otentikasi tanpa membocorkan data ke server.

🔹 W3C DID Standard Compliance

✔ Menggunakan standar DID W3C agar kompatibel dengan sistem lain.

🔹 Blockchain Security

✔ Data DID disimpan di blockchain, tidak bisa diubah oleh pihak ketiga.


Finalisasi & Testing


# Jalankan backend
node server.js

# Register DID
curl -X POST http://localhost:3001/register -H "Content-Type: application/json" -d '{"did": "did:ethr:0x12345", "metadata": "User John Doe"}'

# Resolve DID
curl http://localhost:3001/resolve/did:ethr:0x12345
